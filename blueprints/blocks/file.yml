name: File
icon: file
preview: fields
wysiwyg: true
fields:
  file:
    type: files
    label: Upload or select a file
    max: 1
    required: true
    uploads: false
    help: Upload a new file or select from the file archive.
    query: site.find('file-archive').files
  label:
    label: Label
    type: writer
    inline: true
    width: 5/6
    placeholder: Leave blank to use file name
