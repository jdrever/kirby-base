label: Scheduled Publication
icon: calendar
fields:
  scheduled:
    type: structure
    label: Pages for scheduled publication.
    help: This will happen automatically.  Note that the schedule is checked every five minutes.
    fields:
      page:
        label: Page
        type: pages
      scheduledPublishDate:
        label: Publish Date
        type: date
      scheduledPublishTime:
        label: Publish Time
        type: time
    columns:
      page:
        width: 3/4
      scheduled_publish:
        width: 1/4
