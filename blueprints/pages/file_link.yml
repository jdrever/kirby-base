title: File Link

icon: file-code
image:
  color: green


tabs:
  linkTab:
    label: Link
    fields:
      file:
        type: files
        label: Upload or select a file
        max: 1
        required: true
        uploads: true
        help: Upload a new file or select from the current page or the file archive.
        query: site.find("file-archive").files
  panelTab:
    extends: tabs/panel
    columns:
      main:
        fields:
          showSubPageImages: false
