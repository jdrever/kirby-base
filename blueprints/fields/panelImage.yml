type: files
label: Panel Image.
multiple: false
query: page.images.template('image').add(page('image-bank').images.template('image'))
uploads:
  template: image
crop: true
help: displayed on parent page
